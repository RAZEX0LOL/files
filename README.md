# 🤖 Универсальный Telegram-бот для бизнеса

Демо-бот для портфолио на Kwork. Полный функционал: запись клиентов, приём заказов, заявки, уведомления админу, рассылка, статистика.

## Возможности

- **📋 Каталог услуг** — список услуг с ценами и описанием
- **📅 Онлайн-запись** — пошаговая запись на услугу (выбор услуги → дата → время → комментарий)
- **🛒 Заказы** — приём заказов с адресом доставки
- **📩 Заявки** — свободная форма обращения
- **👤 Мои записи** — клиент видит свои записи и статусы
- **🔔 Уведомления** — админ получает уведомления о каждой новой записи/заказе/заявке
- **📊 Админ-панель** — статистика, управление записями, рассылка
- **💾 SQLite** — все данные хранятся в базе, ничего не теряется

## Установка на Ubuntu Server (чистый сервер)

### Шаг 1: Получи токен бота

1. Открой Telegram, найди **@BotFather**
2. Отправь `/newbot`
3. Введи имя бота (например: `Мой Демо Бот`)
4. Введи username бота (например: `my_demo_business_bot`)
5. Скопируй токен (формат: `1234567890:ABCdefGhIjKlMnOpQrStUvWxYz`)

### Шаг 2: Узнай свой Telegram ID

1. Найди бота **@userinfobot** в Telegram
2. Нажми `/start`
3. Скопируй свой ID (число, например: `123456789`)

### Шаг 3: Загрузи файлы на сервер

**Вариант А — через scp (с локального компьютера):**
```bash
scp -r ./telegram-bot-demo user@ВАШ_IP:~/telegram-bot-demo
```

**Вариант Б — через git (если залил в репозиторий):**
```bash
git clone https://github.com/ваш-юзернейм/telegram-bot-demo.git
```

**Вариант В — вручную:**
Создай папку на сервере и скопируй файлы через SFTP (FileZilla, WinSCP).

### Шаг 4: Установка (автоматическая)

```bash
cd ~/telegram-bot-demo
chmod +x setup.sh
./setup.sh
```

Скрипт установит: Node.js 20, PM2, зависимости, настроит автозапуск.

### Шаг 5: Настрой .env

```bash
nano .env
```

Заполни:
```
BOT_TOKEN=1234567890:ABCdefGhIjKlMnOpQrStUvWxYz
ADMIN_ID=123456789
BUSINESS_NAME=Название Бизнеса
```

Сохрани: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 6: Запусти бота

```bash
pm2 start bot.js --name telegram-bot
pm2 save
```

Готово! Бот работает 24/7 и автоматически перезапускается.

## Команды PM2

| Команда | Описание |
|---------|----------|
| `pm2 status` | Статус всех процессов |
| `pm2 logs telegram-bot` | Логи бота в реальном времени |
| `pm2 restart telegram-bot` | Перезапуск бота |
| `pm2 stop telegram-bot` | Остановка бота |
| `pm2 delete telegram-bot` | Удаление процесса |

## Команды бота

### Для клиентов (кнопки меню):
- **📋 Услуги и цены** — список всех услуг
- **📅 Записаться** — пошаговая запись
- **🛒 Сделать заказ** — оформление заказа
- **📩 Оставить заявку** — свободная форма
- **📞 Контакты** — контактная информация
- **👤 Мои записи** — история записей

### Для админа:
- `/admin` — открыть админ-панель
- **📊 Статистика** — общая статистика
- **📋 Новые записи** — непросмотренные записи с кнопками подтверждения
- **📣 Рассылка** — рассылка всем пользователям

## Структура файлов

```
telegram-bot-demo/
├── bot.js          # Основная логика бота
├── database.js     # Работа с SQLite базой
├── package.json    # Зависимости
├── setup.sh        # Скрипт установки
├── .env.example    # Шаблон переменных окружения
└── README.md       # Эта документация
```

## Настройка услуг

По умолчанию добавлены демо-услуги. Чтобы изменить, отредактируй массив `demoServices` в `database.js` и удали файл `data.db` для пересоздания базы.

## Технологии

- **Node.js 20** — среда выполнения
- **Telegraf** — фреймворк для Telegram Bot API
- **better-sqlite3** — быстрая встроенная база данных
- **PM2** — менеджер процессов для автозапуска
- **dotenv** — переменные окружения
